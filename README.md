# Flutter WebView Project

Это мобильное приложение на Flutter, предназначенное для отображения веб-интерфейса (WebView) страницы авторизации.

## 🚀 Особенности

* Интеграция **WebView** с использованием `webview_flutter`.
* Поддержка JavaScript (необходима для современных веб-приложений).
* Настроенные разрешения для доступа в интернет.
* Оптимизировано для работы с [данным URL](https://steady-cucurucho-4feb7a.netlify.app/auth/login).

---

## 🛠 Настройка и установка

### 1. Требования

* Flutter SDK: `^3.0.0`
* Dart SDK: `^3.0.0`
* Android API Level: `21+`

### 2. Конфигурация Android

Для корректной работы интернета и WebView на Android были внесены следующие изменения:

**Разрешения (`AndroidManifest.xml`):**

```xml
<uses-permission android:name="android.permission.INTERNET" />

```

**Минимальная версия SDK (`android/app/build.gradle`):**

```gradle
defaultConfig {
    minSdkVersion 21
}

```

### 3. Запуск проекта

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-логин/название-репозитория.git

```


2. Перейдите в папку проекта:
```bash
cd название-репозитория

```


3. Установите зависимости:
```bash
flutter pub get

```


4. Запустите приложение:
```bash
flutter run

```



---

## 📦 Используемые зависимости

| Пакет | Версия | Описание |
| --- | --- | --- |
| `webview_flutter` | ^4.0.0 | Основной плагин для отображения веб-контента |

---

## 📂 Структура проекта

* `lib/main.dart` — точка входа и основная логика контроллера WebView.
* `android/` — конфигурация манифеста и сборки для Android.

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

---

### Советы по наполнению:

1. **Скриншоты:** Обязательно добавьте скриншот работающего приложения в раздел "Особенности". Для этого создайте папку `assets/` в корне и положите туда файл, а в README пропишите: `![App Screenshot](assets/screenshot.png)`.
2. **iOS:** Если планируете запускать на iPhone, добавьте в README блок про `Info.plist` и ключ `NSAppTransportSecurity`.

